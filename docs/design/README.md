# Detailed designs

Deep dives for flows/components too complex for the one-page summary in
[`../architecture.md`](../architecture.md) §4/§12. Created via the
`architecture-doc` agent / `/document-architecture` prompt using
`.github/skills/architecture-docs/references/detailed-design-template.md`,
only when the `architecture-docs` skill's threshold for one is actually
met — most changes don't need one. Each file here should be linked from
`../architecture.md` §12; an orphaned doc nobody can find from the index
is close to useless.
